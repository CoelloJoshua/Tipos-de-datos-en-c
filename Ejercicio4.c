#include <stdio.h>
#include <stdlib.h>

/*Escribir un programa que tome dos números
 enteros x e y como argumentos e imprima la distancia entre el punto (x, y) y el origen (0, 0).
 */
int main(int argc, char *argv[]) {
int x = atoi(argv[1]);
int y = atoi(argv[2]);


  printf("%s %s", argv[1] ,argv[2]);
  return 0;

}
